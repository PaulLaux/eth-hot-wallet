// import { fromJS } from 'immutable';
// import { makeSelectHeaderDomain } from '../selectors';

// const selector = makeSelectHeaderDomain();

describe('makeSelectHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
