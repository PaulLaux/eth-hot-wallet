// import React from 'react';
// import { shallow } from 'enzyme';

// import SendConfirmationView from '../index';

describe('<SendConfirmationView />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
